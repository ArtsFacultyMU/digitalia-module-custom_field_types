name: 'digitalia_custom_field_types'
type: module
description: 'Custom field types used in Digitalia MUNI Arts. Each field type is implemented as separate submodule that has to be enabled to use it.'
package: custom
core_version_requirement: ^10 || ^11
